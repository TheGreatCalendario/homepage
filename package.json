{
  "name": "therapiepapst",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -r app/scripts dist/ && cp -r app/styles dist/",
    "preview": "vite preview",
    "test": "karma start test/karma.conf.js --single-run"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "karma": "^6.4.4",
    "karma-chrome-launcher": "^3.2.0",
    "karma-firefox-launcher": "^0.1.6",
    "karma-jasmine": "^5.1.0",
    "jasmine-core": "^5.1.2",
    "puppeteer": "^23.0.0"
  }
}
